import picker from './datePicker';
/*
test('blabla', () => {
  expect(typeof picker).toBe("Object");
});
*/

describe('Picker', () => {
  it('picker', () => {
    expect(typeof picker).toBe("object");
  })
});