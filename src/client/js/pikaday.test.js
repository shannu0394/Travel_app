import picker from './pikaday';
/*
test('blabla', () => {
  expect(typeof picker).toBe("Object");
});
*/

describe('Picker', () => {
  it('picker', () => {
    expect(typeof picker).toBe("Object");
  })
});